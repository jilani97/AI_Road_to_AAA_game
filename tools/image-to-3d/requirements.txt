# Base requirements for tools/image-to-3d — installed into .venv (Python 3.11).
#
# PyTorch CUDA 12.1 wheels come from the PyTorch index below. The driver on
# the target machine (581.95) supports CUDA 12.1/12.4/12.6; cu121 is the
# most battle-tested.
--extra-index-url https://download.pytorch.org/whl/cu121

# Core ML runtime (GPU).
torch==2.5.1
torchvision==0.20.1

# HF model hub + transformer backbones (used by TripoSR and Trellis).
transformers>=4.44.0,<5.0
huggingface_hub>=0.24.0,<1.0

# Mesh handling + GLB export.
trimesh>=4.4.0

# Image I/O + preprocessing.
Pillow>=10.4.0
numpy>=1.26.0,<2.0  # NumPy 2.x breaks several ML packages; pin to 1.x until stable
rembg>=2.0.57

# Web UI (Phase 2). Pinned ≥5.0 — Gradio 4.44 had a gradio_client schema
# incompatibility with Pydantic 2.13 ("argument of type 'bool' is not
# iterable") and a flaky localhost-reachability check.
gradio>=5.0

# Test runner.
pytest>=8.3.0

# --- TripoSR pipeline (Task 1.2) ---
# TripoSR upstream lives at https://github.com/VAST-AI-Research/TripoSR and
# has no setup.py / pyproject.toml, so `pip install git+...` doesn't work.
# We clone it into `external/TripoSR/` via `python install_triposr.py`
# and inject the path at import time. These are its transitive runtime deps:
omegaconf>=2.3.0
einops>=0.8.0
xatlas>=0.0.9
imageio>=2.34.0
onnxruntime>=1.18.0
# CPU marching-cubes fallback — avoids the torchmcubes CUDA+MSVC build.
# PyMCubes exposes `import mcubes`; the pipeline monkey-patches it under
# the `torchmcubes` name so TripoSR's hard import resolves.
PyMCubes>=0.1.6
scikit-image>=0.24.0
